{
  "name": "truth-lens-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:web": "pnpm --filter web dev",
    "dev:ext": "pnpm --filter extension dev",
    "build": "pnpm -r build"
  },
  "pnpm": {
    "overrides": {
      "sharp": "0.33.5"
    }
  }
}